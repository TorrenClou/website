---
title: Configuration
description: Every setting in the app, every environment override, and which one wins.
---

Almost everything is in the app, under **Settings**. Environment variables exist only for the
handful of things the container cannot work out for itself.

**Nothing here is required.** A `docker run` with no `-e` flags and no `--env-file` is a complete
configuration.

## In the app

### Account

Change your password. Requires the current one; minimum 12 characters.

### Transfers

| Setting | Default | What it does |
|---------|---------|--------------|
| Reroute failed uploads | on | When a drive stops accepting uploads, move the job to another healthy one instead of failing it |
| Reroute attempts per job | 3 | Cap on automatic reroutes. Stops one job walking every drive you own when the real fault is the file |
| Failures before a drive is unhealthy | 3 | Consecutive upload failures on one drive before it stops being offered |
| Concurrent transfers | 10 | **Restart required.** How many torrents run at once |

Concurrent transfers is the one to understand. A download occupies a worker for its entire
transfer, so this is a hard ceiling: with it set to 10, the eleventh torrent waits, however idle
the machine looks. Raise it if you have bandwidth and disk to spare; lower it on a small VPS.

Changes to the others take effect within about 30 seconds, with no restart.

### Downloads

**Delete downloads after upload** (on by default) removes a job's local directory once every file
has reached your cloud storage. Turn it off to keep local copies — they then only go away when
you purge.

### Local Storage

Shows what the downloads volume holds, split three ways:

- **Purgeable** — directories for completed and cancelled jobs. What **Purge** deletes.
- **Retained** — jobs still running, retrying or failed. Kept, because those jobs may still need
  the files.
- **Orphaned** — directories with no matching job. Counted so the space is visible, never deleted
  automatically.

**Purge** only ever removes the purgeable set.

## Environment variables

**None of these are required.** A `docker run` with no `-e` flags and no `--env-file` is a
complete configuration — the container generates what it needs on first boot.

This table is generated from the backend's own option declarations and attached to each release,
so it describes what the code implements rather than what someone remembered to write down. It
used to be maintained by hand here, in two READMEs, in `deploy/docs` and in `.env.example`, and no
two copies agreed.

<ConfigSchemaVersion />

### Required by the application

These have no built-in default. The all-in-one image generates both on first boot and keeps them
on the `torrencloud-pgdata` volume, which is why you never set them by hand — but a split
deployment, where nothing generates them, must supply them.

<ConfigTable filter="required" />

### Optional overrides

Setting one overrides the built-in behaviour for that value only.

<ConfigTable filter="optional" />

### Legacy

Only for installs that predate the first-run wizard.

<ConfigTable filter="deprecated" />


## Precedence

For the three settings that exist in both places — worker count and the two observability
toggles — the order is:

1. The value saved in **Settings**, read at container start
2. The environment variable
3. The built-in default

So once you save a worker count in the app, `HANGFIRE_WORKER_COUNT` no longer has any effect.

## Using a .env file

You do not need one. If you would rather keep overrides in a file:

```bash
docker run -d --env-file .env ... ghcr.io/torrenclou/torrentclou:latest
```

[`.env.example`](https://github.com/TorrenClou/deploy/blob/main/.env.example) lists the available
overrides, all commented out.
